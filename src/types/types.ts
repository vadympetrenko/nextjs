export enum Role {
    casier = 'cashier',
    manager = 'manager',
    sushi_chef = "sushi chef",
    cook = 'cook'
}

export enum WorkingType {
    full_time = 'full time',
    part_time = "part time"
}

export enum adminStatus {
    pending = "pending",
    approved = "approved",
    declined = 'declined'
  }

  export enum employeeStatus {
    read = "read",
    unread = "unread",
  }  